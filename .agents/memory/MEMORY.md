- [Amvera pnpm build](amvera-pnpm-build.md) — root package.json must pin `packageManager: pnpm@10.x` or corepack grabs pnpm 11 in Docker and ignores `onlyBuiltDependencies`; build success ends at "Pushing image", rest is runtime env.
- [Session auth vs Clerk](session-auth-migration.md) — this project migrated off Clerk to custom email+password + DB-backed sessions; the old Clerk localization note no longer applies.
- [No email provider configured](no-email-provider.md) — password reset and similar flows return the link in-app instead of emailing it; check before assuming email delivery works.
- [Deploy workflow](deploy-workflow.md) — deploying == pushing to GitHub (Amvera auto-builds); use `./deploy.sh "message"` since main agent's shell blocks `git push`.
- [Session/token expiry boundary semantics](session-expiry-boundary.md) — a row seeded with expiresAt=now IS deleted by lt(expiresAt, new Date()) cleanup queries, since real time always advances before the query runs.
- [Amvera amvera.yaml real schema](amvera-yaml-schema.md) — no `ports` list field exists at all, only `run.containerPort` (single int); clean build log does not mean config is valid, check app log too.
- [Amvera internal DB TLS verification](amvera-internal-db-tls.md) — sslmode=require against self-signed CNPG cert breaks every query identically (looks like missing schema, isn't); fix with rejectUnauthorized:false.
- [VPN node seeding](vpn-node-seeding.md) — key issuance 404s if `vpn_nodes` is empty; all-in-one deploy needs one row seeded from REALITY_* env vars, not automatic.
- [Amvera raw TCP port limitation](amvera-raw-tcp-port.md) — containerPort's comma-separated list is HTTP-only; confirmed Amvera always terminates TLS on 443 itself, no raw TCP passthrough for Reality/VLESS without Dedicated IPv4. Resolved via VLESS+WebSocket (see file for details).
- [VPN subscription URL design](vpn-subscription-links.md) — self-updating subscription link (stateless HMAC token, base64 body, branded headers) replaces raw pasted vless links; key labels must never contain user email.
- [Xray gRPC proto loading & stats](xray-grpc-proto-loading.md) — protobufjs needs resolvePath override for include-root-relative proto imports; QueryStats(reset:true) works, GetUsersStats is UNIMPLEMENTED; Xray "email" tag must be a unique id (UUID), never a user-facing label.
- [Artifact re-registration on re-import](artifact-reregistration.md) — listArtifacts() empty + proxy 502 despite intact artifact.toml files? createArtifact for one artifact re-registers the others too.
- [openapi.yaml spec drift — resolved](openapi-spec-drift.md) — never hand-edit files under `generated/`; always add schema to openapi.yaml first, then rerun orval codegen, or clients silently diverge from spec and each other. Also: never name a component schema exactly `<operationId>Body/Params/Response/QueryParams` — orval auto-generates that same identifier per-operation and it collides.
- [Drizzle array params + subscription fan-out](drizzle-array-params-and-subscription-fanout.md) — `sql\`= any(${arr}::int[])\`` fails via drizzle even though raw pg works; use inArray() instead. Resolve "current" row per user via selectDistinctOn before joining, or 1:many fan-out multiplies sums.
- [Amvera prod DB unreachable from sandbox](amvera-prod-db-unreachable-from-sandbox.md) — internal cnpg hostname is ENOTFOUND outside Amvera's cluster; a valid connection-string secret can't fix a network-reachability problem.
- [Admin user delete/role safety](admin-user-delete-safety.md) — cascade-delete must abort (not best-effort) if Xray key deprovisioning fails, and both delete + role-demote must block removing the last remaining admin.
- [Xray traffic polling: reset:false + lastSeen](xray-traffic-lastseen.md) — reset:false + DB lastSeen closes crash/restart loss windows; flush call sites must be serialized to prevent race-driven double-credit.
- [Check deploy state before verifying prod](check-deploy-state-before-verifying.md) — merged code can sit undeployed to Amvera indefinitely; always confirm no pending commits before trusting production matches HEAD.
- [pnpm workspace: db package has stale dist/ despite source exports](workspace-db-stale-dist.md) — tsc project-reference resolves lib/db's compiled dist/*.d.ts, not its `exports` (which point to src); after editing lib/db schema, rebuild it (`pnpm --filter @workspace/db exec tsc -p .`) or downstream tsc sees old types.
- [VPN primary domain failover](vpn-primary-domain-failover.md) — vpnexus.pro vs Amvera technical domain chosen server-side via admin-editable payment_settings.primaryDomain + cached healthz check; links regenerated per-request, not baked.
- [drizzle-kit push --force still prompts on rename ambiguity](drizzle-push-force-rename-prompt.md) — column drop+add or adding unique constraint to a table with data prompts even with --force in non-TTY. Fix: pre-create via raw SQL (heal-schema.mjs), declare as uniqueIndex() in schema (not .unique() on column) so drizzle-kit sees an index not a constraint.
- [Multiple "active" rows + null-first DESC ordering](multi-active-rows-null-first-desc.md) — every place that assigns a new "active" status to a row must also retire prior actives in the same transaction; separately, never `ORDER BY desc(nullableCol)` to pick "the current one" since Postgres sorts NULLs first in DESC.
- [Slot/entitlement lifecycle tied to subscription row](slot-lifecycle-subscription-row.md) — per-cycle purchased entitlements (e.g. extra device slots) belong on the subscription row, not the user, so they expire naturally on renewal/switch instead of needing manual cleanup.
- [Manual-payment key-revoke race](manual-payment-key-revoke-race.md) — paid users could end up with 0 VPN keys because manual SBP confirmation lag raced the expiry-revoke sweep; fixed via ensureActiveKeyForUser + grace period, both required.
- [referral_code seed collision](referral-code-seed-collision.md) — inserting `users` directly without an explicit referral_code collides on the shared `""` unique default; test fixtures/E2E seeding/manual psql all need an explicit unique code.
- [payment_settings fallback drift](payment-settings-fallback-drift.md) — the "no row yet" literal in GET /payment-settings must gain a default for every new required settings field, or the endpoint 500s on fresh installs.
- [Traffic-limit entitlement pattern](traffic-limit-entitlement-pattern.md) — trafficLimitExceededAt/extraTrafficGb/revokedReason on subscriptions mirror the extra-device-slot pattern; /me's trafficLimitGb is now the *effective* (plan+extra) cap, not the raw plan value.
- [Hourly billing race](hourly-billing-race.md) — fixed 2026-07-15 with an optimistic lock on last_billed_at (update-then-check-affected-rows before debiting).
- [Traffic-limit bypass via resubscribe](traffic-limit-bypass-via-resubscribe.md) — fixed the hourly-activation half (now retires prior active subscription atomically); plan-switch/renewal clearing the flag remains intentional by design.
- [Payment screenshot validation](payment-screenshot-validation-gap.md) — FIXED 2026-07-16: MIME allowlist (jpeg/png/webp) + magic-byte check + 8MB base64 size cap added to PATCH /payments/:id/screenshot before DB write.
- [Session/reset token hashing](session-reset-token-plaintext.md) — FIXED 2026-07-16: session & password-reset tokens now SHA-256 hashed before DB insert; lookup/delete also use hash. Existing sessions invalidated on deploy (one-time logout).
- [Docker root user](docker-root-user.md) — Amvera all-in-one container has no USER directive, runs Node+Xray as root. Found in full audit 2026-07-14, unfixed, low urgency.
- [Admin vpn-key issue route fixed](admin-vpn-keys-issue-fix.md) — routes through issueKeyForUser (respects node capacity); bypasses user slot/traffic-limit as admin override; if traffic limit already exceeded, background job revokes the key on next tick (documented in code).
- [Check-then-act races in issuance/orders](check-then-act-races.md) — keyIssuance FOR UPDATE tx; free-slot atomic SQL; pending-payment unique index; confirmPayment startsAt/endsAt + currentActive re-read inside FOR UPDATE tx (all 2026-07-16).
- [VPN key revoke write order](vpn-key-revoke-write-order.md) — FIXED 2026-07-16: both user and admin revoke routes write DB first (revokedAt), then Xray (non-fatal if fails).
- [Schema gaps: FK indexes/ON DELETE/uniqueness/numeric types](schema-missing-fk-indexes-and-constraints.md) — indexes added 2026-07-16 (payments.subscriptionId, vpnKeys.nodeId, subscriptions.planId, users.referredByUserId); unique indexes on vpnKeys.uuid + vpnNodes.name + pending-payments partial; onDelete + bigint still unfixed.
- [admin/payments.ts duplicates confirmPayment.ts](admin-payments-duplicates-confirmpayment-lib.md) — FIXED 2026-07-16: admin confirm route now delegates to confirmPaymentById(); keep it that way.
- [Remaining auth hardening gaps](security-hardening-gaps.md) — scrypt reverted to N=16384 (N=65536 crashes Amvera with OOM on every login + invalidates existing hashes); in-memory rate limit + no CSRF still unfixed.
- [FreeKassa integration quirks](freekassa-integration.md) — "API"-badged FK method IDs (36/44) silently become FKWallet; use СБП i=42 via API, card via pay.fk.money form; Amvera swallows any 5xx.
- [Remaining infra hardening gaps](infra-hardening-gaps.md) — HEALTHCHECK + USER node added 2026-07-16 (vpn-node uses port 443 → root required there); resource limits, log rotation, Postgres backup still unfixed.
